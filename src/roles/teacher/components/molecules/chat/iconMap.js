import { ClipboardList, FileText, Lightbulb, PenLine, Sparkles } from 'lucide-react';

export const promptIconMap = {
  clipboard: ClipboardList,
  file: FileText,
  lightbulb: Lightbulb,
  pen: PenLine,
  sparkles: Sparkles,
};
